/* tslint:disable:no-unused-variable */

import { Router } from '@angular/router';
import { RouterTestingModule } from '@angular/router/testing';
import { TestBed, async, inject } from '@angular/core/testing';
// import { fakeAsync, async, , TestBed, getTestBed } from '@angular/core/testing';

import { AppService } from './../../core/services/app.service';
import { Contact } from '../../features-modules/contacts/models/contact';
import { ValidationManager } from '../../shared/models/validation-manager';

describe('Contacts App: Validation', () => {
  // let router: Router;
  // let appService: AppService;
  // let validationManager: ValidationManager;
  // let model = new Contact();
  // beforeEach(() => {
  //     router = jasmine.createSpyObj('Router', ['navigate']);
  //     appService = new AppService(null, router, null);
  //     validationManager = new ValidationManager(appService);
  // });
  // it('should create an instance', () => {
  //     expect(model).toBeTruthy();
  // });
  // it('should validate empty object with errors', () => {
  //     validationManager.resetErrors();
  //     let result = model.validate(validationManager);
  //     expect(result.isValid).toBeFalsy();
  // });
});
